function coingame(coins)
{
while(coins>0)
  {
    if(100 == (math.floor(math.random()*100)+1)
        {
          return coins+(51 +math.floor(math.random()*50));
        }

    coins--;
  }
  return 0;
}
